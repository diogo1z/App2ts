import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SelecionarClinicaPage } from './selecionar-clinica';

@NgModule({
  declarations: [
    SelecionarClinicaPage,
  ],
  imports: [
    IonicPageModule.forChild(SelecionarClinicaPage),
  ],
})
export class SelecionarClinicaPageModule {}
